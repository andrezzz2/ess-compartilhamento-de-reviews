const database = require('../../../database');

beforeAll(() => {
    database.connect();
});

afterAll(() => {
    database.disconnect();
});


describe((''), () => {

    //testar erro interno
    it((''), () => {



    });

});