const User = require('../models/User');

/*
const someDocument = undefined;
afterEach(async () => {     
    Document.deleteMany({_id: someDocument._id});
});
*/

describe((''), () => {

    it((''), () => {

    });
    test((''), () => {

    });
});