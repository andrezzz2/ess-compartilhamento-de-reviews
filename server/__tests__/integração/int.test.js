const User = require('../../models/User');

//depois dos testes vai ter que desfazer alterações
afterAll(() => {

});

describe((''), () => {

    it((''), () => {

    });
    test((''), () => {

    });
});